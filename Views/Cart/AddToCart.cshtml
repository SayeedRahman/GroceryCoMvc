@model GroceryCoMvc.Models.ShoppingCartVM

@{
	ViewBag.Title = "CartAdd";
	int itemNo = 1;
	int productNo = 1;			
}

<h2>CartAdd</h2>
<p>
    @Html.ActionLink("Checkout", "Receipt")
</p>
<p>
    @Html.ActionLink("Truncate", "Delete")
</p>
<table>
    <tr>
        <th>
            No.
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShoppingCartItem.Product.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShoppingCartItem.Product.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShoppingCartItem.Product.Price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.ProductList)
{
    <tr>
        <td>
            @productNo
						@{
							productNo++;		
						}		
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
				
				<td>
					 @Html.ActionLink("AddToCart", "SumToCart", new { id = item.ProductId })
				</td>	
				@*
				@using (Html.BeginForm("AddToCart", "Cart", FormMethod.Post, new { productId = item.ProductId }))
		{
					@Html.ValidationSummary(true)
					@Html.HiddenFor(modelItem => item.ProductId)
					@Html.HiddenFor(modelItem => item.Price)		
					<td>
            <input type="submit" value="AddToCart" />
				</td>	
		}
		*@
				@*
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductId })
        </td>
				*@
    </tr>
}

</table>

<hr />
<table>
    <tr>
        <th>
            Item No.
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShoppingCartItem.Product.Name)
        </th>       
        <th>
            @Html.DisplayNameFor(model => model.ShoppingCartItem.Product.Price)
        </th>
				<th>
            @Html.DisplayNameFor(model => model.ShoppingCartItem.Quantity)
        </th>
				<th>
            @Html.DisplayNameFor(model => model.ShoppingCartItem.Total)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.CartItemList)
{
    <tr>
        <td>
            @itemNo
						@{
	itemNo++;		
						}
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Name)
        </td>
				<td>
            @Html.DisplayFor(modelItem => item.Product.Price)
        </td>
		
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        
				<td>
            @Html.DisplayFor(modelItem => item.Total)
        </td>	
				@*
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductId })
        </td>
				*@
    </tr>
}

</table>

@*
@using (Html.BeginForm()) {
					@Html.ValidationSummary(true)
					@Html.HiddenFor(modelItem => item.ProductId)
					@Html.HiddenFor(modelItem => item.Price)				
				<td>
            <input type="submit" value="AddToCart" />
				</td>
		}
*@